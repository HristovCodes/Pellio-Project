@model Pellio.Models.OrdersList

@{
    ViewData["Title"] = "Cart";
}

<link rel="stylesheet" href="~/css/tailwind.css" />
<link rel="stylesheet" href="~/css/products.css" />
<div>
    @{ decimal all_price = 0;}
    @foreach (var item in Model.Products)
    {
        <div class="parent items-center ml-32">
            <div class="div1">
                <img class="h-48 w-48 rounded-full border-2" src="@item.ImageUrl" alt="@item.ProductName" />
            </div>
                <div class="div2">
                    @item.ProductName
                </div>
                <div class="div3">
                    @item.Price
                    @{ all_price += item.Price;}
                </div>
        </div>
        <br />
    }
    <br />
    <div class="flex flex-row content-center justify-center items-center">
        <div>
            <h1 class="flex-1 mr-24">Total:</h1>
        </div>
        <div class="flex flex-row items-center ">
            <div class="text-center flex-1 hidden">
                @Model.Products.Count
            </div>
            <div class="text-center flex-1">
                @all_price
            </div>
        </div>
    </div>
</div>
